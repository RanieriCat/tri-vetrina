---
import LeadWizard from '@/components/LeadWizard.astro';
import MainLayout from '@/layouts/MainLayout.astro';
import TrustSection from '@/components/TrustSection.astro';

const mosaicTiles = [
  {
    title: 'Editoriale Luce Naturale',
    tag: 'Magazine',
    image: 'https://picsum.photos/seed/photo-mosaic-01/1400/1000',
    size: 'l'
  },
  {
    title: 'Campagna Capsule',
    tag: 'Commerciale',
    image: 'https://picsum.photos/seed/photo-mosaic-02/1000/900',
    size: 'm'
  },
  {
    title: 'Ritratto Soft',
    tag: 'Portrait',
    image: 'https://picsum.photos/seed/photo-mosaic-03/900/1200',
    size: 's'
  },
  {
    title: 'Evento Serale',
    tag: 'Live',
    image: 'https://picsum.photos/seed/photo-mosaic-04/1400/1000',
    size: 'm'
  },
  {
    title: 'Product Still',
    tag: 'E-commerce',
    image: 'https://picsum.photos/seed/photo-mosaic-05/1200/900',
    size: 's'
  },
  {
    title: 'Story Brand',
    tag: 'Lifestyle',
    image: 'https://picsum.photos/seed/photo-mosaic-06/1400/1000',
    size: 'm'
  }
];

const chapters = [
  {
    chapter: 'Capitolo 01',
    title: 'Matrimoni Intimi',
    body: 'Racconto discreto della giornata: dettagli, persone e luce ambiente con ritmo naturale.',
    meta: 'Consegna demo: teaser social e selezione estesa in gallery privata.',
    image: 'https://picsum.photos/seed/photo-story-01/1400/900'
  },
  {
    chapter: 'Capitolo 02',
    title: 'Ritratto Professionale',
    body: 'Set guidato per founder, team e creator: posa naturale, direzione semplice e coerenza visiva.',
    meta: 'Consegna demo: headshot, mezzi busti e formato verticale per profili.',
    image: 'https://picsum.photos/seed/photo-story-02/1400/900'
  },
  {
    chapter: 'Capitolo 03',
    title: 'Commerciale Prodotto',
    body: 'Scatti orientati alla conversione: texture leggibili, color grading coerente e varianti ADV.',
    meta: 'Consegna demo: marketplace, social ads e hero shop.',
    image: 'https://picsum.photos/seed/photo-story-03/1400/900'
  },
  {
    chapter: 'Capitolo 04',
    title: 'Eventi & Backstage',
    body: 'Sequenza dinamica in tempo reale: momenti chiave, atmosfera e recap pronto alla pubblicazione.',
    meta: 'Consegna demo: set rapido per PR, newsroom e social recap.',
    image: 'https://picsum.photos/seed/photo-story-04/1400/900'
  }
];
---
<MainLayout template="photo" title="Luca Ferretti | Fotografia Editoriale e Commerciale" description="Produzioni fotografiche per brand, ritratti ed eventi con storytelling visivo e consegne pronte per web e social.">
  <section class="photo-hero photo-texture" aria-labelledby="photo-hero-title">
    <img
      class="photo-hero-media"
      data-photo-parallax
      src="https://picsum.photos/seed/photo-hero-editorial/2400/1400"
      alt="Fotografo al lavoro durante uno shooting editoriale"
      width="2400"
      height="1400"
      fetchpriority="high"
      decoding="async"
    />
    <div class="photo-hero-vignette" aria-hidden="true"></div>

    <div class="container photo-hero-content" data-reveal>
      <p class="eyebrow">Fotografo editoriale e commerciale</p>
      <h1 id="photo-hero-title" class="photo-hero-title" data-variable-type>
        Immagini che raccontano e convertono.
      </h1>
      <p class="photo-hero-lede">
        Produzioni fotografiche per brand, professionisti ed eventi: direzione chiara, stile coerente e consegne pronte per web, social e stampa.
      </p>
      <div class="hero-actions">
        <a class="btn" href="#photo-lead">Richiedi disponibilita</a>
        <a class="btn btn--ghost" href="/photo/portfolio">Guarda il portfolio</a>
      </div>
    </div>
  </section>

  <TrustSection
    eyebrow="Segnali di fiducia"
    title="Affidabilita chiara sopra la piega"
    intro="Clienti, metriche e testimonianza visibili subito: un impianto utile a comunicare solidita e facilitare la conversione."
    logos={['Studio One', 'Brand Two', 'Agency Three', 'Event Four']}
    metrics={[
      { value: 'XX+', label: 'Produzioni annue (demo)', note: 'Placeholder' },
      { value: 'YY%', label: 'Clienti ricorrenti (demo)', note: 'Placeholder' },
      { value: 'ZZ h', label: 'Tempo medio di risposta (demo)', note: 'Placeholder' },
      { value: 'AA/5', label: 'Valutazione servizio (demo)', note: 'Placeholder' }
    ]}
    quote="Testimonianza placeholder: direzione creativa chiara, shooting fluido e materiali coerenti con il brand."
    quoteAuthor="Cliente brand (placeholder)"
  />

  <section class="section container" data-reveal>
    <div class="section-head">
      <p class="eyebrow">Mosaic editoriale</p>
      <h2>Griglia dinamica ma ordinata</h2>
      <p class="section-intro">Tre formati ricorrenti (L/M/S) per mostrare lavori diversi mantenendo continuita visiva.</p>
    </div>

    <div class="mosaic" aria-label="Anteprima portfolio modulare">
      {mosaicTiles.map((tile, index) => (
        <article class={`mosaic-tile mosaic-tile--${tile.size} photo-texture photo-breath ${index === 0 ? 'tilt-accent' : ''}`} data-reveal data-tilt={index === 0 ? 'true' : undefined}>
          <img
            src={tile.image}
            alt={tile.title}
            width="1400"
            height="1000"
            loading="lazy"
            decoding="async"
          />
          <div class="mosaic-meta">
            <p>{tile.tag}</p>
            <h3>{tile.title}</h3>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="section container" data-reveal>
    <div class="section-head">
      <p class="eyebrow">Story sequence</p>
      <h2>Portfolio raccontato per capitoli</h2>
      <p class="section-intro">Scroll narrativo in 4 blocchi: ogni capitolo mostra obiettivo, stile e tipo di consegna.</p>
    </div>

    <div class="story-sequence">
      {chapters.map((chapter, index) => (
        <article class={`story-chapter ${index % 2 === 1 ? 'story-chapter--reverse' : ''}`} data-reveal>
          <figure class={`story-media photo-texture ${index === 2 ? 'tilt-accent' : ''}`} data-tilt={index === 2 ? 'true' : undefined}>
            <img
              src={chapter.image}
              alt={chapter.title}
              width="1400"
              height="900"
              loading="lazy"
              decoding="async"
            />
          </figure>
          <div class="story-copy">
            <p class="eyebrow">{chapter.chapter}</p>
            <h3>{chapter.title}</h3>
            <p>{chapter.body}</p>
            <p class="muted">{chapter.meta}</p>
          </div>
        </article>
      ))}
    </div>
  </section>

  <LeadWizard
    id="photo-lead"
    title="Raccontami il progetto da fotografare"
    intro="Tre passaggi rapidi per condividere obiettivo, contesto e tipo di shooting."
    needs={['Campagna brand', 'Ritratto professionale', 'Evento / reportage']}
    submitLabel="Invia richiesta"
  />

  <script is:inline>
    (() => {
      const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      const heroMedia = document.querySelector('[data-photo-parallax]');
      const variableTitle = document.querySelector('[data-variable-type]');
      let ticking = false;

      const applyScrollEffects = () => {
        const progress = Math.min(window.scrollY / 440, 1);

        if (heroMedia instanceof HTMLElement) {
          heroMedia.style.transform = `scale(1.06) translate3d(0, ${progress * 26}px, 0)`;
        }

        if (variableTitle instanceof HTMLElement) {
          const weight = Math.round(780 - progress * 220);
          const track = (-0.055 + progress * 0.022).toFixed(3);
          variableTitle.style.setProperty('--photo-wght', String(weight));
          variableTitle.style.setProperty('--photo-track', `${track}em`);
        }

        ticking = false;
      };

      const onScroll = () => {
        if (reduceMotion || ticking) return;
        ticking = true;
        window.requestAnimationFrame(applyScrollEffects);
      };

      if (!reduceMotion) {
        applyScrollEffects();
        window.addEventListener('scroll', onScroll, { passive: true });
      }

      const canTilt = !reduceMotion && window.matchMedia('(pointer: fine)').matches;
      if (!canTilt) return;

      const tiltTargets = Array.from(document.querySelectorAll('[data-tilt]'));
      tiltTargets.forEach((target) => {
        if (!(target instanceof HTMLElement)) return;

        const onMove = (event) => {
          const rect = target.getBoundingClientRect();
          const x = (event.clientX - rect.left) / rect.width - 0.5;
          const y = (event.clientY - rect.top) / rect.height - 0.5;
          target.style.setProperty('--tilt-y', `${(x * 6).toFixed(2)}deg`);
          target.style.setProperty('--tilt-x', `${(-y * 6).toFixed(2)}deg`);
        };

        const reset = () => {
          target.style.setProperty('--tilt-y', '0deg');
          target.style.setProperty('--tilt-x', '0deg');
        };

        target.addEventListener('pointermove', onMove);
        target.addEventListener('pointerleave', reset);
      });
    })();
  </script>
</MainLayout>
